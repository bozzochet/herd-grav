enabled: true
auth:
    method: 'api'   # Can be either `api` or `password`. Empty for no Authentication [limit is 60 per hour]
    token: '6ebe039f0101c68fa026e9e98ccc90810570d216'       # If `api` method, token is the API generated on GH, otherwise the username
#    password: ''    # Only required when method is `password`, leave empty otherwise
